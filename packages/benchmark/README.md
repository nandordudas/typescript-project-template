# Typescript project template - Benchmark

Run benchmarks. Only `.suite.ts` ended files would be used.

These benchmarks can be run under GitHub actions, select `benchmark` workflow and `Run workflow`.
